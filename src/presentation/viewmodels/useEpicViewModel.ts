import { useCallback } from "react";
import { useAppDispatch } from "../hooks/useContextHook";

export const useEpicViewModel = () => {
    const dispatch = useAppDispatch();

    // Acceder al estado de epic
}